Prepare a clean handoff so the next session can start with full context. Do the following in order:

**1. Update `docs/progress.md`:**
- Check off any Phase A items completed this session (change `[ ]` to `[x]`)
- Replace "Last Session" with 3–6 bullet points covering: what was built/changed, any important context, gotchas discovered, state of anything left mid-flight
- Replace "Next Steps" with the 2–3 most important things to tackle next, in priority order
- Update "Open Questions / Blockers" — remove resolved items, add anything unresolved
- Keep the total file under 80 lines. "Last Session" and "Next Steps" are always replaced, never appended to.

**2. Update `docs/decisions.md`:**
For any significant decision made this session (architectural, schema, naming, scope call, deviation from design doc), append an entry using the format at the top of that file. If no decisions were made, skip this step.

**3. Confirm:**
After updating both files, print a brief summary of what you wrote so I can verify it looks right before ending the session.
