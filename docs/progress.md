# arogya — Session Context

> Loaded automatically at every session start. Update with `/handoff` before ending a session.
> Full decision log: `docs/decisions.md`

---

## Current Phase: Phase E — AI-Driven Flows (active) — Phase D ✓ COMPLETE (all 13 surfaces + rail). Phase E checklist expanded below.

### Phase A (complete)
- [x] 1-7. Foundation shipped (see decisions.md 2026-05-11/12 entries).

### Phase B — Agent Infrastructure
- [x] 1. Vault context builder — `lib/agents/_shared/vault-context.ts`. `excludeBriefs` default-true, `includeInsights` mode, `surfaceContext`, deterministic `now` param.
- [x] 2. Per-entity serializers — 14 files in `lib/agents/_shared/serializers/` + `format.ts` + barrel.
- [x] 3. Agent error types — `lib/agents/_shared/errors.ts`. `AgentError` with discriminated `code`.
- [x] 4. Zod output schemas — `lib/agents/_shared/schemas.ts`. All agent outputs per 10.3.
- [x] 5. Synthesis agent — `lib/agents/synthesis.ts` + `app/api/chat/route.ts`. Opus 4.7, streaming, verbatim prompt, cache hit verified (3150 tokens), three-modes-from-prompt.
- [x] 6. Router — `lib/agents/router.ts` + `scripts/check-router.ts`. Haiku 4.5, direct SDK, no vault context, 3-bucket confidence. Smoke green (6/6 cases, ~1s latency per call).
- [x] 7. Citation parser — `lib/citations/{parse,remark-plugin}.ts` + `components/{citation-pill,ai-message}.tsx` + `scripts/check-citation-parser.ts`. react-markdown + custom remark plugin → inert pills with `data-*` attrs for Phase C handoff. Smoke green (10/10 cases incl. round-trip against `citationFor()`).

### Phase C — First Vertical Slice (Medication) ✓ COMPLETE — reference impl for Phase D
- [x] 1. API routes — `app/api/medications/*` + `lib/schemas/api/medication.ts` (path-less, auth-derived patientId; PATCH refuses clinical fields; transactional `/discontinue` + `/changes`; `/by-slug/[slug]` resolver matches `slugify(name)`, no stored slug).
- [x] 2-3. Form (`components/medications/medication-form.tsx`, RHF+Zod, 6.12; `lib/schemas/forms/medication.ts` via `.extend()`) + list page (6.4: server fetch + Map denormalization, client islands for filters/section-collapse).
- [x] 4-5. Detail page (6.5: 5 sections, parallel fetch, 8+ components; collapse only when changes>5, `…` omitted when discontinued, Notes omitted when empty) + inline-edit (`inline-field`, useState/onBlur) + `+ Log a change` (transactional change-log write).
- [x] 6. Ask AI push-drawer — `components/chat/*` + `app/patient/[id]/layout.tsx`; single persistent `useChat` thread across patient, non-modal/no-backdrop/push layout, `surfaceContext` rides each message; `lib/chat/surface-context.ts`.
- [x] 7. Citation-pill popover — `§ med:<slug>` → fetch-on-open preview (name·dose·freq·status) → `View full →`. Non-med + external pills inert (their detail pages are Phase D).

### Phase D — Replicate Pattern (active)
Reuse Phase C's Medication template. **State entities** → 6.4 list + 6.5 detail + 6.12 form + `*_changes` log (Medication is the reference impl). **Event entities** → NEW templates 6.6 timeline + 6.7 event detail (no change log; Outcomes replaces History; Lab markers read-only). Sequencing: do **Condition** first (proves the state template generalizes beyond Medication), then **Visit** first among events (proves the new event template), then the rest parallelize.
**Per-entity build order** (clone Medication files, don't re-derive): schema (`lib/schemas/api/*`) + `db/queries/*` → API routes (curl-smoke green) → form → list page → detail page → inline-edit + change-log. That's Phase C items 1→5. **Items 6-7 are now infra, NOT per-entity work:** the Ask AI drawer is global; citation pills just need a `bySlug` query + `/api/<entity>/by-slug/[slug]` route + a case in the pill resolver. **Shared infra exists (use it, don't re-clone):** `lib/api/route-helpers.ts` (`coerceChangedAt`/`parseJsonBody`/`validateUuidParam`/`fieldErrorsFromReason`), `db/queries/_shared.ts` (`*InScope` FK scope-checks), `lib/logger.ts` (log every server_error catch).

State entities (6.4 + 6.5 + `*_changes`):
- [x] Condition — **COMPLETE** (API · form · list · detail · inline-edit · +Log a change · Delete · live `§ condition:` pill). 2nd proof the state template generalizes. Detail shipped `0f34d25`.
- [x] Doctor — **COMPLETE** (API · form · list grouped by specialty · detail w/ backlinks · +Log a change [specialty/clinic] · Delete [409 when visits exist] · live `§ doctor:` pill). E2 + D2 unlocks landed with it (see Last Session).
- [x] Allergy — **COMPLETE** (API · form · list grouped by status · detail · +Log a change [status/severity] · Delete · live `§ allergy:` pill). 4th proof; thin-spec calls in decisions.md 2026-06-10 (NOT a rail item — list is §6.10 profile-reached; no Linked context until Phase E insights).
- [x] Lifestyle — **COMPLETE** (singleton: GET/PATCH-upsert `/api/lifestyle` + `/changes` · NO list/new/Add-form · §6.5:1403 narrative blocks + 4-col strip · 7 trend fields change-logged, first-population-via-PATCH then locked · live `§ lifestyle:profile` pill). Shipped `d1058ab`.
- [x] FamilyHistory — **COMPLETE** (API · form · list grouped by relation [CHILDREN bucket added] · detail [NO History; name-match Linked context; H1=condition] · all-fields inline edit · Delete · live `§ family-history:` pill). Shipped `d1058ab`. **State-entity column DONE.**

Event entities (6.6 + 6.7; no change logs):
- [x] Visit — **COMPLETE** (API · Log-visit form [9 fields, zero-doctors guard] · §6.6 month-grouped timeline [date-anchored cards · dotted separator · most-recent tint · ↑/≡/+ outcome badges · doctor filter · show-earlier at month boundary] · §6.7 detail [NOTES FROM VISIT w/ markdown bold · Outcomes from `linked_visit_id` backlinks · FK-linked-episode Linked context · full inline edit, no refusal map] · Delete w/ outcome-detach copy · live `§ visit:` pill). Event template PROVEN. **Committed to main.**
- [x] LabReport + LabResults — **COMPLETE** (API [nested create txn · `/results/[resultId]` correction PATCH] · Log-lab-report form w/ `useFieldArray` markers sub-form · §6.6 month-grouped timeline [marker-preview cards, flagged-first; `All report types ▾` filter; no most-recent tint] · §6.7 detail [Summary · read-only MARKERS table + `+ Log a correction` dialog · `⚠ N flagged` Outcomes → scroll-to-markers · monitored-conditions + previous-panels Linked context · report-level inline edit] · `△` SLIGHTLY HIGH/LOW/CRITICAL pills (critical→`destructive` red, high/low→new `--warning` amber) · Delete cascades markers · live `§ lab-report:` pill). First one→many split. **Committed to main.**
- [x] Symptom (Type + Episode) — **COMPLETE** (API [`/api/symptom-types/*` + `/api/symptom-episodes/*`; episode POST creates type inline in one txn] · Log-symptom form [`+ Create new` type · datetime-local · duration/severity/description/triggers/relief · single linked-vital select] · §6.6 **type-grouped** timeline [collapsible groups, active expanded; severity pills; `●` linked-vital pills; most-recent tint; per-group show-earlier] · §6.7 episode detail [EPISODE NOTES · "Captured at this episode" linked vitals · parent-link chip + nearby-episode Linked context · datetime/number inline-edit] · **NEW SymptomType state-detail page** `/symptoms/types/[id]` [status/condition/notes edit · episode stream] · live `§ symptom:` + `§ symptom-episode:` pills · **post-hoc vital link/unlink in episode Edit** · **'+ Log episode' from a type locks the symptom + returns there**). Inverted one→many. **Committed.**
- [x] Report — **COMPLETE** (API [`?reportType=` filter param matches Lab API] · `Log report` form [text-only; linked visit+doctor selects] · §6.6 month-grouped timeline w/ `All types ▾` enum filter [no tint, no badges] · §6.7 detail [`REPORT CONTENT` markdown body · Outcomes = meds/conditions via `source_report_id`, omit-when-empty · Linked context = linked visit · "from {doctor}" subtitle · full inline edit, scope-checked linked FKs] · Delete [outcomes detach via set-null] · live `§ report:` pill). **Source-file upload + PDF preview + `status` lifecycle DEFERRED to Phase E** (user-scoped); Outcomes data lands when extraction writes `source_report_id`. **Committed.**
- [x] JournalEntry — **COMPLETE** (API · `New entry` form [title opt · date · content · **mood** added] · §6.6 month timeline [no filter, no tint, "written by you", title-less cards show content preview] · §6.7 detail [`ENTRY` markdown · **NO Outcomes / NO Notes** · Linked context = `LINKED ENTITIES` resolved pills + `OTHER ENTRIES THIS MONTH` siblings · `(untitled)` fallback · full inline edit] · Delete · live `§ journal:` pill). `linked_entities` resolver built (covers all detail-page types; `vital`/unknown dropped) — **empty until Phase E AI-tagging**, proven via DB seed. **Event-template family DONE (5/5).** **Committed.**
- [x] VitalReading — **COMPLETE** (create-only: `/api/vital-readings` POST/GET/DELETE + Log-reading form at `/vitals/new`; NO timeline/detail page; surfaces as `●` pills on episodes; `§ vital:` inert; entry point = Phase E; **DELETE scrubs the id from episodes' `linked_vital_ids`** so no dangling refs). Committed w/ Symptom.

Standalone surfaces:
- [x] Patient profile — **COMPLETE** (§6.10 state-detail variation: `patients.notes` col + migration · `relationship` on auth stub · `patientQueries.{update,atAGlance}` · singleton `PATCH /api/patient` · IDENTITY/MEDICAL PROFILE/AT A GLANCE/NOTES, full inline edit, disabled `…` menu + photo affordance, Ask AI). **+ refinements:** units inferred from `patients.country` (`lib/units.ts`, metric stored, imperial proven via `units:check`); allergies de-duped to MEDICAL PROFILE only; AT A GLANCE rows enriched with sample names, still a plain link list. **CONTACT + primary language DEFERRED** (no schema cols, per user); weight `history→` trend deferred (no vitals-history surface); insights AT-A-GLANCE row text-only until §6.8 lands. **Committed.**
- [x] Insights feed + detail — **COMPLETE (uncommitted)** (§6.8 status-grouped feed [**2 multi-select filter pills** §6.8:1593 · severity-dot cards · `EntityListSections` NEW+SEEN open] · §6.9 read-only detail, **all 5 sections** [live `§`-pill body · prominent status actions Acknowledge/Mark-acted-on/Dismiss/Restore, NO green · Cited sources · derived Linked-context "OTHER PATTERNS WITH" · **Notes** inline-editable] · `GET`+`PATCH /api/insights/[id]` [status any→any + notes; no `*_changes`] · `insights.notes` col [migration `0002`, applied] · shared `db/queries/entity-links.ts` resolver [journal delegates] · vital-triggered triggered-by labeled inertly §6.9:1610 · **`db/seed.ts` expanded** w/ §1.3 demo mini-vault + 5 insights, seed applied). **Closes the Phase D entity checklist.**
- [x] **Health Wiki rail — COMPLETE (uncommitted)** (§3 + §6.1:1146: `components/patient/wiki-rail.tsx` + `db/queries/wiki-counts.ts` in the patient layout — branding + patient switcher · Insights/Chat top-level · nine HEALTH WIKI items + live counts + active-state). **Dashboard/RECENT omitted, Chat→drawer, ⌘K search inert** (unbuilt surfaces / v1-excluded — no dead links; FLAGGED).

### Phase E — AI-Driven Flows (after D) — the 6 differentiating capabilities (§10.2 step E)
Per §10.2: last because they depend on everything before. **Schema groundwork already shipped in Phase A** — `reports.status` enum (extracting/ready/failed/committed), nullable `source_report_id` on the 7 derivable entities (medication·condition·lab·vital·visit·symptom·journal), and `extraction_sessions` table all exist. So Phase E is agents + pipeline + surfaces, no new clinical schema. **Build order — now formal §10.2 (resequenced to 9 items, 2026-06-25):** E1 extraction+router-wiring → E2 upload → E3 confirmation → E5 insight-gen → **E0a dashboard** → **E0b full-screen chat + `chat_sessions`** → E4 onboarding → E6 auto-titling → E7 doctor brief. Items E1–E5 are the surface-free extract→confirm→commit→insight loop (lands the §1.3 north-star earliest, against the existing seed vault); E0a/E0b are the two foundational surfaces §10.2 previously left unscheduled, each followed by the agents that depend on it. Tripwires that bite here: extraction NEVER auto-writes (human-in-loop confirm, §5.4); insights dedup-only on prior insights, reason fresh (§5.6 echo-chamber warning); insight gen is debounced fire-and-forget, no queue (§9.3). **Each new agent's prompt is composed inline (§9.3) from its §10.3 structural spec — persona · brand-voice rules · output format · hard constraints** (extraction §10.3:3193, insight-gen :3225, onboarding :3241, auto-title :3263; doctor-brief = synthesis brief-mode addendum). Output schemas already locked in `_shared/schemas.ts` (Phase B4).

**Spec inconsistency to resolve before E1/E3 (design.md internal):** §5.4:763 and the locked Zod `extractionOutputSchema` use `{intent, target_entity_type, matched_entity_id, extracted_data, ambiguities: string[], source_excerpt}`, but §10.3:3201 describes a different shape (`{type, bucket, extracted_fields, existing_entity_match, ambiguities:[{field,question,options}]}`). The schema (Phase B) followed §5.4 and wins — EXCEPT `ambiguities`: §6.11's confirmation UI (inline-resolve chips + `(pending)` field linkage, E3) needs the **structured** `{field, question, options}` form, which the flat `string[]` can't drive. Decide: extend the schema's `ambiguities` to the structured shape (recommended — unblocks E3) and doc-fix §5.4 to match §10.3. Needs sign-off (output-schema change).

**Foundational surfaces — now numbered §10.2 items 5 & 6 (block downstream items):**
- [ ] **E0a. Dashboard (§6.1)** — chat-centerpiece + status strip + suggested actions + recent insights feed. **Onboarding exits here and the activation banner is onboarding's only launcher (§6.3:1246)** — so Dashboard must exist before E4. Activation-banner *logic* (incomplete-onboarding state) is Phase F2, but the shell is Phase E. (§10.2: "onboarding after the dashboard works".)
- [ ] **E0b. Full-screen chat (§6.2/§6.5) + chat-session persistence** — auto-titling (E6) and findable briefs (E7) need persisted, titled, reopenable sessions; today only the ephemeral push-drawer exists (no chat-session table in v1). **Requires a schema addition (chat_sessions [+ messages] table) → needs explicit sign-off per CLAUDE.md before building.** Also wire D2 deferrals here (grounding header + per-message `grounded in →`).

**The 6 agents/capabilities:**
- [ ] **E1. Extraction agent + router wiring (§5.4, §5.5, §9.3).** Router already built (Phase B6) — wire it on every chat input *before* `runSynthesis` (D3 carryover): `question`→synthesis, `log`→extraction, `ambiguous`→inline 2-button disambiguator. `lib/agents/extraction.ts` Sonnet 4.6, text + vision, JSON `{extractions:[{intent,target_entity_type,matched_entity_id,extracted_data,ambiguities,source_excerpt}]}`, three buckets (confident-match→update / confident-new→create / uncertain→user-picks), matching-dictionary in vault context (`includeInsights:"none"`), never-fabricate guardrails, empty-array on total failure. Zod schema in `_shared/schemas.ts`. Compound input = log first, forward to synthesis post-confirm (§5.5).
- [ ] **E2. File upload pipeline (§9.4).** Two-step signed-URL flow: `/api/files/sign` (returns signed PUT URL + path) → direct browser→Supabase upload → `/api/files/process` (creates Report status=`extracting` + `extraction_sessions` row status=`pending` → `runExtraction` → session `ready_for_confirmation`/`failed`). `arogya` bucket, patient-namespaced paths (Phase A made bucket). `lib/storage.ts`: `getSignedUrl`/`uploadFile`/`deleteFile` (3600s expiry). `FilePreview` client component (image lightbox / PDF iframe + `1 of N`). Report created up-front so file survives extraction failure.
- [ ] **E3. Extraction confirmation surface (§6.11).** Dedicated page `/patient/[id]/extract/[sessionId]` (reads the session row); split-panel (source left ~40% sticky / cards right ~60% scroll); ambiguity prompts at card top with inline-resolve chips (block `Confirm` until resolved, `(pending)` field state); new-vs-update toggle per card (3 buckets, flips preview); card-state pill (`confident`/`needs your call`); per-card Confirm/Discard/`Edit manually instead`→form (§6.12); `Confirm all · N` (greys to `· N blocked`); compound group-by-type headers; failure state (dashed empty + "things that usually help" + `replace file →`); commit writes entities with `source_report_id` set + report status→`committed`; fire debounced insight-gen after commit. **NO floating Ask AI here** (§6.11). Receives BOTH upload-path and quick-log-path extractions.
- [ ] **E4. Onboarding interview (agent §5.8 + surface §6.3).** *Depends on E0a (Dashboard).* `lib/agents/onboarding.ts` Sonnet 4.6, 8 phases in order (patient→conditions→meds→doctors→allergies→events→lifestyle→primary concern), dual output per turn (streamed reply + structured-extraction JSON, same shape as extraction). **Live-transparency, NO confirmation gate** — patient page populates right-side in real time, each card inline-editable, ambiguities resolved in conversation. Interruptible/resumable per phase, no completeness gate, escape chips (`I don't know`/`Upload instead`/`Skip for now`). Phase-1 identity updates the header live. Mid-interview upload→extraction→same live page. Completion → offer first full health scan (routes to full-screen chat with scan as first reply) else dashboard. Surface = chat left + live patient page right (§6.3), `✦` AI marker, chat visuals match §6.2.
- [ ] **E5. Insight generator (agent §5.6 + endpoint §9.3).** `lib/agents/insight-generator.ts` Opus 4.7; the 5 locked insight types (correlation→`pattern`, threshold→`trend`/`improvement`, interaction, gap, risk); conservative severity (urgent rare, most `watch`/`informational`); **empty output is the default & correct**; prior 90-day insights **dedup-only, never evidence** (echo-chamber warning verbatim in prompt); cite every source. Fire-and-forget `/api/insights/generate` with 30s debounce (`getRecentInsightRun`), `db.insert(insights)`, no await on caller. Wire triggers after significant entity creations incl. extraction-commit + form saves. **Replaces the seeded demo insights — ZERO surface change** (feed §6.8 + detail §6.9 already shipped in Phase D). This is the §1.3 north-star demo moment generator.
- [ ] **E6. Auto-titling (§6.2:1217, §9.3).** *Depends on E0b (chat sessions).* Haiku 4.5 call after the first AI response in a session; input = first user msg + first AI reply; output 3-6 word natural-shorthand title; writes `chat_sessions.title`. Non-streaming direct SDK.
- [ ] **E7. Doctor brief capability (§5.7).** NOT a separate agent — synthesis-agent variant in `synthesis.ts` (already three-modes-from-prompt; add `delta`/`handoff` brief-mode addendum, clinical register). Delta vs handoff output structures (§5.7); 1-2 inline clarifying Qs for mode/target; specialty-selective filtering; citations VISIBLE in chat reply / STRIPPED in PDF. `Download PDF` action on the reply → PDF gen (citation-strip + clean clinical black-on-white + "Generated from N data points…" footer). No saved Brief entity (lives in the chat session — needs E0b). Sparse-data → declines.

**Carryover audits queued for Phase E (decisions.md):** broaden `outcomesForReport` to all 7 `source_report_id` entities (§9.4 — data lands once extraction writes the FK; reconcile §6.7 Outcomes vs §9.4 Linked-context placement, decisions.md 2026-06-17); journal/insight `linked_entities` AI-tagging (resolver built, empty until now); resolve `surfaceContext` server-side from an entity id (E3 prompt-injection surface); vital chat-quick-log entry point; symptom↔condition dedup; Report source-file upload + PDF preview + `status` lifecycle (deferred from Phase D Report vertical); weight `history→` trend; CONTACT + primary-language cols; rail Dashboard/RECENT links (unblock once E0a lands).

### Phase F — Polish

---

### Last Session
- 2026-06-23/24 — **Insights vertical (§6.8 feed + §6.9 detail) + Health Wiki rail (§3/§6.1) — COMPLETE & COMMITTED. Closes Phase D (Insights was #13).** API `GET`+`PATCH /api/insights/[id]` (status any→any + `notes`; no `*_changes`); `insightQueries`; shared `db/queries/entity-links.ts` (journal delegates); `components/insights/*` (11 files); rail = `wiki-rail.tsx` + `wiki-counts.ts` in layout. `db/seed.ts` expanded w/ §1.3 demo mini-vault (Dr. Sharma · Amlodipine +Apr-3 change · Dizziness +4 morning episodes · 4 BP vitals) + 5 insights — applied to arogya-dev (idempotent).
- **ALL in-vertical §6.8/§6.9 spec deviations fixed:** status grouping (WATCH→SEEN, §6.8:1563 doc-fix); **multi-select filter pills** (§6.8:1593, checkbox menus, comma-list URL); content width (max-w-3xl); **Notes section + `insights.notes` col** (migration `0002` applied, inline-edit commit-on-blur); **vital-triggered triggered-by** labeled inertly (§6.9:1610); terra/clay→periwinkle (design.md doc-fix).
- **Verified hard:** multi-agent workflow (ship-ready, 0 must-fix) + `/check` (4 eng concerns fixed — feed batch-resolve via `resolveEntityRefMap`; layout-comment; counts/backlink scale notes) + **`verify-ui` 23✅·9🔍·0❌** against live seeded data. Tripwires confirmed (auth via `getCurrentPatient()`, backlinks render-derived, no RAG).
- **Not fixable in v1 (deferred, not bugs):** §6.9 RELATED INSIGHTS (no insight↔insight data model till Phase E); §6.9 slug breadcrumb (Phase 9, siblings use UUIDs); rail Dashboard (§6.1)/RECENT (§3) (unbuilt surfaces); ⌘K search (v1-EXCLUDED §10.1 — inert render is correct).
- **Rail-expansion enhancement DECIDED, build DEFERRED (decisions.md 2026-06-24):** every rail section expands to a capped item preview (~5 + `view all N →`), lazy-loaded on expand, header still links to the category page. Reopens §3:203 (tree was v2-deferred) in bounded form.

### Next Steps
1. **Build the rail-expansion enhancement** (decided: "all sections, capped"). Each section expands to ~5 most-recent items + `view all N →`, lazy-loaded on expand (reuse list GETs / a recent-items query), header still navigates to the category page. Layered onto `wiki-rail.tsx`. Doc-fix §3:203 when done.
2. **Phase E** — extraction + upload + confirmation · onboarding · insight *generator* (replaces seed insights, zero surface change) · auto-titling · doctor brief. Audits queued in decisions.md: vital chat-quick-log; symptom↔condition dedup; broaden `outcomesForReport` to 7 entities (§9.4); journal/insight `linked_entities` AI-tagging; weight `history→`; rail Dashboard/RECENT; CONTACT/primary-language cols.

### Open Questions / Blockers
- **Condition active→active re-confirm (v1.5):** no-op status guard (409) also blocks re-confirming the *same* status with a fresh reason/date. Accepted for v1; decisions.md 2026-06-02.
- **Pending doc-fixes (next design.md edit):** (§6.12:1842 + §4:243) form date *defaults* are now browser-local (`todayLocal()`), not patient-tz — user decision 2026-06-10 (§9.6's "display in user's local tz" already agrees); (§6.5) permit Notes omission when empty + broaden "Edit toggles Current fields" to all non-clinical (clinical → +Log a change); (§6.2/§6.4) floating Ask AI = context-preserving push drawer (non-modal), NOT full-screen — rail "Chat" + chat persistence remain Phase D (no chat-session table v1); **(§6.7:1534 vs §9.4:2571/2573) Report-detail derived entities: §6.7 says Outcomes (meds+conditions), §9.4 says Linked context (all 7 `source_report_id` entities) — reconcile placement + breadth when extraction lands (decisions.md 2026-06-17 follow-up)**; **(§6.8:1563) drop the wireframe-era "WATCH" from the expanded-by-default groups — `watch` is a severity, not a status (read as SEEN)**. *(§6.9:1607/1635 terra/clay→periwinkle already applied to design.md; §6.9:1629 Notes now implemented via `insights.notes`.)*
- **Chat-surface deferred:** (D2) §6.2 grounding header + per-message `grounded in →` footer; (D3) §6.2:1197 router-on-every-input not wired (drawer synthesis-only) — add before `runSynthesis` in **Phase E**; (E3) `surfaceContext` is client-asserted free text (prompt-injection surface) — resolve server-side from an entity id when auth is real.
- **Citation-pill limitations (v1.5):** collision-suffixed citations (`med:x-2`/…/`visit:<date>-2` for same-day visits) resolve to null → misleading "not in record".
- **Allergy detail Linked context** intentionally absent until the Phase E insight generator produces med↔allergy interactions (§6.5:1402 has no stored edge behind it). Pending doc-fix candidates: §6.5:1402 "substance prominent" (substance is the H1) + a §6.4 note that the Allergies list isn't rail-reached + a §6.12 note on the missing Add-lifestyle form (first-population-via-PATCH fills the gap until onboarding) + §6.4:1336 FH CHILDREN bucket.
- **Phase D carryovers (non-blocking):** **Medication inline-field missing `items` on its category/form Selects** → shows raw enum in edit mode (the patient field hit + fixed this; Medication still has it). **Lifestyle v1.5:** real tags input for dietRestrictions (comma-split text for now); change-log dietRestrictions if tag transitions prove meaningful; profile delete/reset path (none — by design). Also: **doctor pickers on Add forms** (§6.12 rich autocomplete + `+ Create new` inline modal for med `prescribingDoctor`/`purpose` + condition `diagnosedBy`/`managingDoctor`) still deferred — every ref IS settable post-create (log-change dialogs / inline select); **list-card entity refs stay inert text** (cards are whole-card anchors; §6.4:1348 wants embedded links, §6.6:1488 wants clickable visit badges — same card-restructure pass); lab outcome rows + linked-episode rows link-less (report outcome rows now link out — `View medication/condition →`); med change-entry "Linked:" annotation inert (med Linked-context section carries the link); Older: enum-validation → `lib/schemas/url-filters.ts`; `useId()` panel IDs; drawer `32rem` magic-number; `chat-drawer-provider` does 4 jobs; **periwinkle dark-mode (v1.5)** no `.dark`; **SDK** `@ai-sdk/anthropic` 3.0.x missing top-level `cacheReadInputTokens`.

*Keep this file under 80 lines. Last Session and Next Steps replace on each `/handoff` — never append.*
