Run the following commands in sequence from the project root and report results:

1. npx tsc --noEmit
2. npx eslint .

If both pass clean, say so clearly. If there are errors or warnings, list them grouped by file with line numbers. Do not fix anything — just report what you find.
